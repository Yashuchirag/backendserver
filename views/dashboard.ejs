<!DOCTYPE html>
<html lang="en">
<%- include("includes/head") %>
<body>

    <%- include("includes/header") %>
    <% if (posts.length === 0) { %>
        <main>
            <h4>Hello <%= user.username %>,</h4>
            <p>You have no posts yet</p>
        </main>
    <% } else { %>
        <main>
            <h4>Hello <%= user.username %>, here are your posts</h4>
            <ul>
              <% posts.forEach(post => { %>
                <li>
                  <a href="/post/<%= post.id %>"><%= post.title %></a> —
                  <small>by <%= post.author %>, <%= new Date(post.createdDate).getMonth() + 1 %>/<%= new Date(post.createdDate).getFullYear() %></small>
                </li>
              <% }) %>
            </ul>
          </main>
    <% } %>
    <%- include("includes/footer") %>
</body>
</html>